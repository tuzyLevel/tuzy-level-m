namespace BallTranslateSimulation {
  
    function solution(
    n: number,
    m: number,
    x: number,
    y: number,
    queries: [number, number][]
  ) {
    const curPos = [x, y];
    while(true){
        
        const [direction, offset] = queries.pop() as [number, number];
        if(direction >=2){//행 연산
            if()



        }else{//열 연산

        }
        




    }
    



  }
}
