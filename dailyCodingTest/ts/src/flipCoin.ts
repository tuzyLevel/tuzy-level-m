namespace flipCoin {
  class Coin {
    coins;
    constructor(coins: number[][]) {
      this.coins = coins;
    }

    flip(x: number, y: number) {

    }
  }

  const solution = (beginning: number[][], target: number[][]) => {};
}
